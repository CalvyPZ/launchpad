`icon.svg` is the canonical icon for the PWA manifest.

`icon-192.png` and `icon-512.png` are still needed for full iOS support and some older installers.

When available, generate PNG exports from `icon.svg` with a converter (for example Inkscape or any online SVG-to-PNG tool) and drop them into this folder.
